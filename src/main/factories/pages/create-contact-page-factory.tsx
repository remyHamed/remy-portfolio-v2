import { CreateContactPage } from "../../../presentation/pages/create-contact-page/create-contact-page";

export const makeCreateContactPageFactory :React.FC = (): React.ReactNode => {
    return (
        <CreateContactPage/>
    );
}